/** Workflow runner compatibility exports.
 *
 * The implementation lives in `src/shared/worker-runner.ts` so direct workers
 * and workflow agents execute through the same child-agent runtime.
 */
export * from "../shared/worker-runner.ts";
