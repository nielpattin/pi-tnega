/** Workflow runner compatibility exports.
 *
 * The implementation lives in `src/shared/agent-runner.ts` so direct workers
 * and workflow agents execute through the same child-agent runtime.
 */
export * from "../shared/agent-runner.ts";
