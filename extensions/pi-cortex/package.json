{
   "name": "pi-cortex",
   "version": "0.1.0",
   "type": "module",
   "scripts": {
      "build:rust": "cd rust-embedder && cargo build --release",
      "test": "vitest run",
      "check": "pnpm test && tsc --noEmit -p tsconfig.json",
      "typecheck": "tsc --noEmit -p tsconfig.json"
   },
   "dependencies": {
      "@earendil-works/pi-tui": "^0.83.0"
   },
   "peerDependencies": {
      "@earendil-works/pi-coding-agent": "*",
      "@sinclair/typebox": "*"
   },
   "pi": {
      "extensions": [
         "./index.ts"
      ]
   }
}
